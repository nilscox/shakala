{
  "name": "backend-domain",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "mocha",
    "lint": "eslint src"
  }
}
