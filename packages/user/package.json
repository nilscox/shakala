{
  "name": "@shakala/user",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "tsc -b",
    "lint": "eslint src",
    "ts-node": "NODE_NO_WARNINGS=1 node --loader ts-node/esm",
    "test": "pnpm run ts-node --test --test-reporter spec",
    "test:watch": "pnpm run ts-node --watch --test-reporter spec"
  },
  "dependencies": {
    "@shakala/common": "workspace:*",
    "brandi": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.14.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
