{
  "name": "shakala",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/0-shared",
    "packages/1-backend-domain",
    "packages/2-backend-application",
    "packages/3-backend-infrastructure",
    "packages/4-frontend-domain",
    "packages/5-frontend-client",
    "packages/6-e2e"
  ],
  "scripts": {
    "clean": "yarn workspaces run clean",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint",
    "spellcheck": "cspell --locale en,fr 'packages/*/src/**'",
    "build:shared": "yarn workspace shared build",
    "build:backend": "yarn workspace backend-infrastructure build",
    "build:frontend": "yarn workspace shared build && yarn workspace frontend-client build",
    "build": "yarn build:shared && yarn build:backend && yarn build:frontend",
    "typecheck": "yarn build:backend && yarn workspace frontend-client tsc -b",
    "start:backend": "yarn workspace backend-infrastructure start",
    "start:frontend": "yarn workspace frontend-client start"
  },
  "devDependencies": {
    "@nilscox/expect": "^0.2.3",
    "@nilscox/expect-sinon": "^0.1.3",
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.7.16",
    "@types/sinon": "^10.0.13",
    "@typescript-eslint/eslint-plugin": "^5.36.2",
    "@typescript-eslint/parser": "^5.36.2",
    "esbuild": "^0.15.11",
    "esbuild-register": "^3.3.3",
    "eslint": "^8.23.0",
    "eslint-import-resolver-typescript": "^3.5.1",
    "eslint-plugin-import": "^2.26.0",
    "mocha": "^10.0.0",
    "sinon": "^14.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
